## Plugin Configuration
---
package:
  name: data-search
  description: The data-search panel and associated widgets
  type: iframe
install:
  route:
    mode: auto
    routes:
      - path: "?q=:-q"
        view: search
        authorization: true
  menu:
    mode: auto
    items:
      - name: search2
        path: []
        label: Search2
        icon: search
